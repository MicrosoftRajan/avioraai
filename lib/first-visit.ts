/** Cookie: set when the user leaves the first-visit landing for the normal home. */
export const RETURNING_VISITOR_COOKIE = "aviora_returning";

export const MARK_RETURNING_SEARCH_PARAM = "mark_returning";

export const continueHomeHref = `/?${MARK_RETURNING_SEARCH_PARAM}=1`;
